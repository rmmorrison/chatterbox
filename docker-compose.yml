version: '3.8'

services:
  mariadb:
    image: mariadb:10.5
    volumes:
      - db_data:/var/lib/mysql
    ports:
      - "3306:3306"
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: kwyjibo

volumes:
  db_data: {}